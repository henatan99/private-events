<%= form_with(model: event) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <div class ="event">
			<h2>Create Event</h2>
			<p>
				<%= form.label :name, claass: 'label' %>
				<%= form.text_field :name %>
			</p>
			<p>
				<%= form.datetime_select :starts_at %>
			</p>
			<p>
				<%= form.label :description,claass: 'label' %>
				<%= form.text_area :description,cols: 40, rows: 5 %>
			</p>
			<div class="actions">
    		<%= form.submit 'Create', class: 'event_create' %>
  		</div>
    </div>
  </div>
<% end %>
